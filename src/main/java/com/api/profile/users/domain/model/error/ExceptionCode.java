package com.api.profile.users.domain.model.error;

/**
 * An enum to represent the general exception codes for the application.
 */
public enum ExceptionCode {
  INVALID_INPUT,
  INVALID_ID,
  DUPLICATED_RECORD,
  PARAMETER_NOT_PROVIDED,
  NOT_FOUND,
  UNAUTHORIZED,
  INVALID_PASSWORD
}
